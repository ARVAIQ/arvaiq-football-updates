# ARVAIQ Admin 1.0.1 assets

Esta pasta recebe apenas os artefactos independentes do Admin:

- `ARVAIQAdmin.exe`
- `ARVAIQAdmin.sha256`

O workflow valida o executável e o SHA-256 antes de ativar `channels/admin.json`.
